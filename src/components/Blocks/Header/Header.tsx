import { ReactElement } from 'react';
import { HeaderContainer } from './styled';

export function Header(): ReactElement {
  return (
    <HeaderContainer>
      <h1>Tasker 🤨</h1>
    </HeaderContainer>
  );
}
