import styled from 'styled-components';
import { ContainerPadding } from '../../styles/styleConstants';

export const BordContainer = styled.div`
  padding: ${ContainerPadding};
  display: flex;
  gap: 3rem;
  height: 90vw;
`;
